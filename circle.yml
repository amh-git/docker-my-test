# The deployment section should be excluded until
# test it is explicitly included in module 5
deployment:
  dockerhub:
    branch: master
    commands:
    - $docker_hub_triger

test:
  override:    
    - mocha
